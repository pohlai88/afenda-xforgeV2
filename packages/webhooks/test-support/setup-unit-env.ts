/** Package-local hook — unit env is owned by repo root test-support. */
// @boundaries-ignore: repo-level test-support is the shared Vitest bootstrap.
import "../../../test-support/setup-unit-env.ts";
