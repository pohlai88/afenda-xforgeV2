/** Footer on the canvas below the floating inset — not inside the raised panel */
const dashboardPageFooterClass = [
  "flex h-full w-full shrink-0 items-center justify-between gap-4",
  "bg-transparent",
  "py-2.5 text-[11px] text-text-secondary antialiased",
].join(" ");

export { dashboardPageFooterClass };
