{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "node -e \"console.log('@repo/typescript-config: preset-only, skip tsc')\""
  }
}
